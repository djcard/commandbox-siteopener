/**
 *
 */
component {

    this['moduleMapping'] = 'OpenMySite'; // cflint ignore:line
    this['modelNamespace'] = 'OpenMySite'; // cflint ignore:line
    this['cfmapping'] = 'OpenMySite'; // cflint ignore:line

    /**
     *
     */
    void function configure() {
    }

}
